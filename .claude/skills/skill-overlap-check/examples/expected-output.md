# Expected Output — スキル重複チェック結果

---

## ケース1: 完全重複あり

### 入力
「実装後にhooksの依存配列漏れを確認するスキルを追加したい」

### 出力
```
## スキル重複チェック結果

### 判定: 既存スキルで対応可能

`hooks-reviewer` エージェントが同じ目的をカバーしています。

- **既存の仕組み**: `hooks-reviewer` エージェント
- **重複内容**: useEffect等の依存配列・副作用の分離を確認する
- **推奨**: 新規作成は不要。チェック観点が不足している場合は improve-skills（またはagentの直接編集）で拡張してください。
```

---

## ケース2: 部分重複あり（拡張推奨）

### 入力
「RPCレスポンスの型アサーション(as)を検出するスキルを追加したい」

### 出力
```
## スキル重複チェック結果

### 判定: 既存スキルへの追記を推奨

`typescript-reviewer` エージェントが型安全性チェックを担っていますが、`as` によるアサーション検出は明示的な項目になっていません。

- **既存の仕組み**: `typescript-reviewer` エージェント
- **重複内容**: 型安全性の観点
- **差分**: `as` アサーションの検出が明文化されていない
- **推奨**: `typescript-reviewer` に「`as` によるレスポンス型アサーションの検出」を追記するか、新規スキルとして分離するか検討してください。
```

---

## ケース3: 新規作成が妥当

### 入力
「設計案を複数提示して比較するスキルを追加したい」

### 出力
```
## スキル重複チェック結果

### 判定: 新規作成を推奨

既存スキルに該当なし。新規作成してください。

- **既存スキルとの差分**: 設計案の提示・比較は現在どのスキルもカバーしていない
- **推奨スキル名**: `design-proposal`
- **次のステップ**: create-skills を使って作成してください。
```
