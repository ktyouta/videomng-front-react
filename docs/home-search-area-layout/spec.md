# ホーム動画一覧 レイアウト刷新 仕様書

## 対象
- `src/features/home/components/videolist/searcharea/` 配下（検索エリア）
- `src/features/home/components/videolist/videoarea/default/` 配下（検索前ワードエリア）

## 変更しないこと（既存仕様）
- 検索エリアの機能（キーワード検索・条件指定モーダルでの種別/カテゴリ絞り込み）は変更しない
- 検索前ワードエリアの機能（最近の検索・よく検索するワード・お気に入りワードの表示/クリック/削除、ローカルストレージへの保存）は変更しない
- 検索条件・キーワード履歴を呼び出すhooksのロジック・API連携は変更しない
- 並び替え・フォルダ作成・CSV取込/保存・選択中タグ表示に相当する機能はホームに追加しない

## 変更すること（今回の刷新内容）
- [x] 検索エリア（`HomeSearchArea`）に、外側6%余白＋内側パネルカード（背景`#1c1f26`・枠線`#3a3f4b`・影・角丸12px）の2層構造を導入する
- [x] 検索テキストボックスを固定割合幅から`flex:1`に変更し、カード内の余白を埋める
- [x] 条件指定トリガーを、背景色付きの丸いボタン（ホバーで背景色変化＋浮き上がり）に変更する
- [x] 検索前ワードエリア（`HomeVideoAreaDefault`）に、検索エリアと同じ2層構造・配色のパネルカードを導入する
- [x] 検索前ワードエリアの3セクション間隔を、ブレークポイント間で35px→125pxと大きく飛んでいた値から20pxに統一する
- [x] 「最近の検索」「よく検索するワード」「お気に入りワード」の各行を、固定54%幅の中央寄せから、ラベル＋`flex-wrap`で左詰めに並ぶ行レイアウトに変更する
- [x] 上記3セクションは、対象のキーワードが0件の場合はセクションごと非表示にする
- [x] キーワード文字のホバー色をアクセント色（`#7abaff`）に統一する
- [x] 色・余白の定数は`HomeConst.ts`に独自定数として追加する（お気に入り側の値と同じだが、お気に入り側のファイルからはimportしない）

## 完了条件
- [x] 上記の変更が反映されている
- [x] 既存のキーワード検索・条件指定・履歴保存/削除の動作に変化がない
- [x] 型エラーが増えていない（既存エラー数から増加しない、`features/home`配下で新規エラーなしを確認済み）
